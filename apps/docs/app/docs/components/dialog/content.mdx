import {
  BasicDialogDemo,
  ResultDialogDemo,
  NestedDialogDemo,
} from './demos'

export const basicExampleTabs = [
  {
    label: 'MyPage.tsx',
    lang: 'tsx',
    code: `import { useDialog } from '@woon-ui/react'
import { Dialog } from '@woon-ui/dialog'

function MyPage() {
  const dialog = useDialog()

  return (
    <button
      onClick={() =>
        dialog.open(({ close }) => (
          <Dialog.Root>
            <Dialog.Overlay />
            <Dialog.Content>
              <Dialog.Title>제목</Dialog.Title>
              <Dialog.Description>설명 텍스트</Dialog.Description>
              <Dialog.Close>닫기</Dialog.Close>
            </Dialog.Content>
          </Dialog.Root>
        ))
      }
    >
      열기
    </button>
  )
}`,
  },
]

export const resultExampleTabs = [
  {
    label: 'MyPage.tsx',
    lang: 'tsx',
    code: `const handle = dialog.open<undefined, string>(({ resolve, close }) => (
  <Dialog.Root>
    <Dialog.Overlay />
    <Dialog.Content>
      <Dialog.Title>선택하세요</Dialog.Title>
      <button onClick={() => resolve('yes')}>확인</button>
      <button onClick={close}>취소</button>
    </Dialog.Content>
  </Dialog.Root>
))

const result = await handle.result
// result.status === 'resolved' → result.value === 'yes'
// result.status === 'dismissed' → 오버레이 클릭이나 ESC로 닫힘`,
  },
]

export const nestedExampleTabs = [
  {
    label: 'MyPage.tsx',
    lang: 'tsx',
    code: `const dialog = useDialog()

dialog.open(({ close }) => (
  <Dialog.Root>
    <Dialog.Overlay />
    <Dialog.Content>
      <button onClick={() => dialog.open(/* 두 번째 다이얼로그 */)}>
        다음 다이얼로그
      </button>
    </Dialog.Content>
  </Dialog.Root>
))

// 전체 닫기
dialog.closeAll()`,
  },
]

export const anatomyTabs = [
  {
    label: 'Dialog.tsx',
    lang: 'tsx',
    code: `import { Dialog } from '@woon-ui/dialog'

<Dialog.Root>
  <Dialog.Overlay />
  <Dialog.Content>
    <Dialog.Title />
    <Dialog.Description />
    <Dialog.Close />
  </Dialog.Content>
</Dialog.Root>`,
  },
]

export const dialogParts = [
  {
    name: 'Dialog.Root',
    description: '다이얼로그 전체 구조와 옵션 기본값을 감싸는 루트 컨테이너입니다. `options`와 `onOpenChange`를 지원합니다.',
  },
  {
    name: 'Dialog.Overlay',
    element: '<div>',
    description: '배경 레이어입니다. closeOnOverlayClick이 켜져 있으면 클릭으로 닫힙니다.',
  },
  {
    name: 'Dialog.Content',
    element: '<div role="dialog">',
    description: '포커스 트랩과 ARIA 속성이 적용되는 실제 다이얼로그 본문입니다.',
  },
  {
    name: 'Dialog.Title',
    element: '<h2>',
    description: '제목을 렌더링하고 aria-labelledby를 자동으로 연결합니다.',
  },
  {
    name: 'Dialog.Description',
    element: '<p>',
    description: '본문 설명을 렌더링하고 aria-describedby를 자동으로 연결합니다.',
  },
  {
    name: 'Dialog.Close',
    element: '<button>',
    description: '현재 다이얼로그를 닫는 액션 파트입니다. asChild를 지원합니다.',
  },
]

export const dialogRootRows = [
  {
    name: 'options',
    type: 'Partial<DialogOptions>',
    description: '`overlay`, `trapFocus`, `scrollLock`, `closeOnOverlayClick`, `scrollTarget` 같은 공통 modal 동작을 제어합니다.',
  },
  {
    name: 'onOpenChange',
    type: '(open: boolean) => void',
    description: '다이얼로그가 실제로 열리고 닫힐 때 호출되는 lifecycle 콜백입니다.',
  },
]

export const dialogOptionRows = [
  {
    name: 'overlay',
    type: 'boolean',
    defaultValue: 'true',
    description: '배경을 어둡게 덮는 overlay를 표시합니다.',
  },
  {
    name: 'trapFocus',
    type: 'boolean',
    defaultValue: 'true',
    description: '열린 동안 Tab 포커스가 다이얼로그 내부에서만 순환하도록 강제합니다.',
  },
  {
    name: 'scrollLock',
    type: 'boolean',
    defaultValue: 'true',
    description: '열린 동안 배경 문서의 스크롤을 잠급니다.',
  },
  {
    name: 'closeOnOverlayClick',
    type: 'boolean',
    defaultValue: 'true',
    description: 'Overlay 클릭 시 다이얼로그를 닫습니다.',
  },
  {
    name: 'scrollTarget',
    type: 'string | Element | null',
    defaultValue: 'undefined',
    description: '스크롤을 잠글 대상 요소. CSS 선택자 또는 Element를 전달합니다. dialogPlugin의 scrollTarget이 앱 전역 기본값으로 사용되며, 이 옵션은 호출별로 오버라이드할 때 사용합니다. 미지정 시 document.body에 적용됩니다.',
  },
]

export const dialogPluginRows = [
  {
    name: 'zIndex',
    type: 'number',
    defaultValue: '200',
    description: '다이얼로그 z-index 시작값. 중첩될수록 1씩 증가합니다.',
  },
  {
    name: 'scrollTarget',
    type: 'string | Element',
    defaultValue: 'undefined',
    description: 'scrollLock의 기본 잠금 대상. 미지정 시 document.body에 적용됩니다.',
  },
  {
    name: 'render.alert',
    type: 'ComponentType<AlertRenderContext>',
    defaultValue: '내장 UI',
    description: 'alert() 기본 렌더 컴포넌트를 교체합니다.',
  },
  {
    name: 'render.confirm',
    type: 'ComponentType<ConfirmRenderContext>',
    defaultValue: '내장 UI',
    description: 'confirm() 기본 렌더 컴포넌트를 교체합니다.',
  },
]

export const toc = [
  { id: '설치', label: '설치', depth: 2 },
  { id: '사용-가이드', label: '사용 가이드', depth: 2 },
  { id: '기본-사용법', label: '기본 사용법', depth: 3 },
  { id: '값-반환', label: '값 반환', depth: 3 },
  { id: '중첩-다이얼로그', label: '중첩 다이얼로그', depth: 3 },
  { id: 'api-레퍼런스', label: 'API 레퍼런스', depth: 2 },
  { id: 'usedialog', label: 'useDialog()', depth: 3 },
  { id: 'dialogcontext', label: 'DialogContext', depth: 3 },
  { id: 'dialoghandle', label: 'DialogHandle', depth: 3 },
  { id: '구조', label: '구조', depth: 3 },
  { id: '컴파운드-컴포넌트', label: '컴파운드 컴포넌트', depth: 3 },
  { id: 'dialogroot', label: 'Dialog.Root', depth: 3 },
  { id: 'dialogoptions', label: 'DialogOptions', depth: 3 },
  { id: 'dialogplugin', label: 'dialogPlugin', depth: 3 },
  { id: '커스터마이징', label: '커스터마이징', depth: 2 },
  { id: '스타일', label: '스타일', depth: 3 },
  { id: 'data-속성', label: 'data 속성', depth: 3 },
  { id: '동작과-접근성', label: '동작과 접근성', depth: 2 },
  { id: '키보드-인터랙션', label: '키보드 인터랙션', depth: 3 },
  { id: 'aria', label: 'ARIA', depth: 3 },
]

## 설치

<PackageManagerTabs package="@woon-ui/dialog" />

```ts
import { Dialog, dialogPlugin, alert, confirm } from '@woon-ui/dialog'
import '@woon-ui/dialog/css'
```

## 사용 가이드

Dialog는 기본적으로 `useDialog()`로 열고 제어합니다.
간단한 알림이나 확인 흐름이 필요할 때는 [`alert()`](/docs/components/dialog/alert), [`confirm()`](/docs/components/dialog/confirm) 같은 호출 함수를 사용할 수 있습니다.

```ts
import { useDialog } from '@woon-ui/react'
import { Dialog, dialogPlugin } from '@woon-ui/dialog'
```

<Callout type="note">
  `useDialog()`는 `WoonProvider`와 `dialogPlugin` 등록이 필요합니다. 설정 방법은 [Provider](/docs/provider)를 참고하세요.
  `alert()`, `confirm()`은 Provider 없이 바로 호출할 수 있습니다.
</Callout>

### 기본 사용법

`useDialog()` 훅에서 `dialog.open()`을 호출하면 다이얼로그가 열립니다.
render 함수에 `close`, `resolve`, `update` 등의 컨텍스트가 전달됩니다. Dialog 문서를 처음 읽는다면 이 패턴부터 익히는 게 가장 좋습니다.

<DocExample tabs={basicExampleTabs}>
  <BasicDialogDemo />
</DocExample>

### 값 반환

`dialog.open()`은 핸들 객체를 반환합니다. `result` Promise로 다이얼로그의 결과를 받을 수 있습니다.

<DocExample tabs={resultExampleTabs}>
  <ResultDialogDemo />
</DocExample>

### 중첩 다이얼로그

다이얼로그 안에서 `dialog.open()`을 다시 호출하면 스택으로 쌓입니다.
ESC 키는 가장 위의 다이얼로그만 닫습니다. `closeAll()`로 전체를 닫을 수 있습니다.

<DocExample tabs={nestedExampleTabs}>
  <NestedDialogDemo />
</DocExample>

## API 레퍼런스

### useDialog()

`useDialog()`는 다이얼로그를 열고 제어하는 메서드 객체를 반환합니다.

| 메서드 | 시그니처 | 설명 |
|--------|---------|------|
| `open` | `(render, options?) => DialogHandle` | 새 다이얼로그를 열고 핸들을 반환합니다. |
| `close` | `(id: string) => void` | id에 해당하는 다이얼로그를 `dismissed`로 닫습니다. |
| `closeAll` | `() => void` | 열려 있는 모든 다이얼로그를 닫습니다. |

`open()`의 두 번째 인자 `options`에는 `DialogOptions`의 모든 필드를 부분 적용할 수 있습니다.
`initialData?: TData`를 함께 넘기면 `DialogContext.data`의 초기값으로 사용됩니다.

<Callout type="tip">
  다이얼로그 내부 어디서든 `useWoonDialogContext()`를 호출하면 `close`, `resolve`, `closeAll`, `options`에 접근할 수 있습니다. render prop을 직접 내려보내지 않아도 됩니다.
</Callout>

### DialogContext

`open()`의 render 함수가 인자로 받는 컨텍스트입니다.

```ts
type DialogContext<TData = undefined, TResult = void> = {
  data: TData                                               // initialData로 전달한 초기값
  close: () => void                                         // dismissed로 닫기
  resolve: (value: TResult) => void                         // resolved로 닫기
  update: (next: TData | ((prev: TData) => TData)) => void  // data 업데이트
}
```

`data`를 사용하지 않는다면 제네릭 없이 `DialogContext`만 써도 됩니다.

### DialogHandle

`open()`이 반환하는 핸들입니다. 다이얼로그 바깥에서 제어가 필요할 때 사용합니다.

```ts
type DialogHandle<TData = undefined, TResult = void> = {
  id: string
  close: () => void
  resolve: (value: TResult) => void
  update: (next: TData | ((prev: TData) => TData)) => void
  result: Promise<DialogResult<TResult>>
}

type DialogResult<TResult> =
  | { status: 'resolved'; value: TResult }
  | { status: 'dismissed'; value: undefined }
```

`result`는 닫히는 방식과 무관하게 항상 resolve됩니다. `status`로 어떻게 닫혔는지 구분하세요.

### 구조

Dialog는 `dialog.open()`으로 여는 imperative API와, 내부 콘텐츠를 설명하는 compound parts를 함께 사용합니다.
최소 구조는 아래와 같습니다.

<CodeTabs tabs={anatomyTabs} />

### 컴파운드 컴포넌트

아래 part를 조합해 다이얼로그 구조를 만듭니다.

<PartsTable rows={dialogParts} />

`Dialog.Title`, `Dialog.Description`, `Dialog.Close`는 `asChild` prop을 지원합니다.

```tsx
<Dialog.Close asChild>
  <button className="my-close-button">X</button>
</Dialog.Close>
```

### Dialog.Root

`Dialog.Root`는 공통 modal 동작을 위한 `options` prop과 실제 열림 상태 lifecycle을 알리는 `onOpenChange`를 지원합니다.

<PropsTable rows={dialogRootRows} />

### DialogOptions

<Callout type="tip">
  런타임 옵션은 `Dialog.Root options={{ ... }}` 또는 `dialog.open(render, options)`의 두 번째 인자로 전달합니다.
</Callout>

<PropsTable rows={dialogOptionRows} />

기본값으로도 일반적인 모달은 충분합니다. 옵션은 동작을 바꿔야 할 때만 건드리면 됩니다.

옵션 적용 우선순위는 `dialog.open()` 두 번째 인자 → `Dialog.Root options` 순입니다. 나중에 전달할수록 우선순위가 높습니다.

예를 들어 **배경을 막지 않는 패널**처럼 쓰고 싶다면 `overlay`, `trapFocus`, `scrollLock`을 꺼서 비모달 패턴으로 전환할 수 있습니다.
다만 이 경우 일반 모달과 다르게 동작한다는 점에 유의하세요.

<Callout type="tip">
  **레이아웃 밀림 방지** — 다이얼로그가 열릴 때 스크롤바가 사라지면서 레이아웃이 밀릴 수 있습니다. 전역 CSS에 아래 한 줄을 추가하면 스크롤바 공간이 항상 예약되어 밀림 현상이 사라집니다.

  ```css
  html {
    scrollbar-gutter: stable;
  }
  ```
</Callout>

<Callout type="note">
  **`height: 100dvh` 레이아웃** — `body` 대신 내부 div가 스크롤을 담당하는 구조라면 `scrollLock`이 body에 적용되어 효과가 없습니다. `dialogPlugin`의 `scrollTarget`으로 앱 전역 기본값을 지정하세요. 특정 호출에서만 다른 대상이 필요하면 `dialog.open()`의 두 번째 인자로 오버라이드할 수 있습니다.

  ```tsx
  // Provider 설정 (앱 전역 기본값)
  dialogPlugin({ scrollTarget: '#main-scroll' })

  // 특정 호출에서만 오버라이드
  dialog.open(render, { scrollTarget: '#other' })
  ```
</Callout>

### dialogPlugin

`WoonProvider`에 등록하는 플러그인 옵션입니다.

```tsx
dialogPlugin({
  zIndex: 200,
  scrollTarget: '#main',
  render: {
    alert: MyAlert,
    confirm: MyConfirm,
  },
})
```

<PropsTable rows={dialogPluginRows} />

## 커스터마이징

Dialog는 기능적 CSS와 상태 data 속성을 제공하고, 시각 스타일은 사용자 쪽에서 덮어쓰는 방식을 전제로 합니다.

### 스타일

기본 스타일은 `@woon-ui/dialog/css/dialog`로 적용됩니다. 컴포넌트 파일 상단 또는 전역 CSS에서 한 번만 import하세요.

```ts
import '@woon-ui/dialog/css/dialog'
```

스타일을 바꾸는 방법은 두 가지로 생각하면 됩니다.

- 기본 구조는 그대로 두고, 현재 선택자를 기준으로 값을 덮어쓴다
- 시트, 드로어, 사이드 패널처럼 위치와 모션까지 바꿔야 한다면 아래 CSS를 기준으로 전체 스타일을 가져와 수정한다

아래 코드는 실제 기본 스타일의 출발점입니다. 먼저 `dialog.css`를 보고 시각 스타일을 조정하고,
중앙 정렬이나 애니메이션 방식까지 바꿔야 할 때만 `dialog-base.css`를 함께 수정하는 편이 가장 안전합니다.

- `dialog.css`: 배경, 여백, 라운드 같은 시각 스타일
- `dialog-base.css`: 위치, 오버레이, 애니메이션 같은 동작 관련 스타일

<CodeTabs
  tabs={[
    {
      label: 'dialog.css',
      lang: 'css',
      code: `/* ── Content 시각 스타일 ── */
[data-woon-dialog-content] {
  padding: 1.25rem 1.5rem;
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
}

/* ── Title / Description ── */
[data-woon-dialog-title] {
  display: block;
  margin: 0 0 0.35rem;
  font-size: 1rem;
  font-weight: 600;
}

[data-woon-dialog-description] {
  display: block;
  margin: 0 0 0.7rem;
}`
    },
    {
      label: 'dialog-base.css',
      lang: 'css',
      code: `/* ── 환경 격리 ── */
[data-woon-dialog-overlay],
[data-woon-dialog-content],
[data-woon-dialog-content] *,
[data-woon-dialog-content] *::before,
[data-woon-dialog-content] *::after {
  box-sizing: border-box;
}

/* ── Overlay ── */
[data-woon-dialog-overlay] {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
}

/* ── Content 위치 (center) ── */
[data-woon-dialog-content] {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: min(100%, 28rem);
  font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 1rem;
  line-height: 1.5;
  color: inherit;
}

/* ── 애니메이션 ── */
[data-woon-dialog-overlay][data-state="open"]   { animation: woon-overlay-in 160ms ease-out; }
[data-woon-dialog-overlay][data-state="closed"] { animation: woon-overlay-out 120ms ease-in forwards; }
[data-woon-dialog-content][data-state="open"]   { animation: woon-dialog-in 200ms cubic-bezier(0.16, 1, 0.3, 1); }
[data-woon-dialog-content][data-state="closed"] { animation: woon-dialog-out 140ms ease-in forwards; }

@keyframes woon-overlay-in  { from { opacity: 0; } to { opacity: 1; } }
@keyframes woon-overlay-out { from { opacity: 1; } to { opacity: 0; } }

@keyframes woon-dialog-in {
  from { opacity: 0; translate: 0 8px; scale: 0.98; }
  to   { opacity: 1; translate: 0 0;   scale: 1; }
}
@keyframes woon-dialog-out {
  from { opacity: 1; translate: 0 0;   scale: 1; }
  to   { opacity: 0; translate: 0 6px; scale: 0.98; }
      }`
    }
  ]}
  collapsible
  defaultExpanded={false}
/>

### data 속성

| 속성 | 대상 | 값 |
|------|------|------|
| `data-woon-dialog-overlay` | Overlay | — |
| `data-woon-dialog-content` | Content | — |
| `data-state` | Overlay, Content | `"open"` 또는 `"closed"` |

상태 기반 스타일링 예시:

```css
[data-woon-dialog-content][data-state="open"] {
  animation: my-dialog-in 200ms ease-out;
}
```

## 동작과 접근성

기본 Dialog는 **모달**로 동작합니다. 별도 설정 없이 아래 동작이 자동으로 적용됩니다.

- 포커스가 다이얼로그 내부에 머뭅니다
- 닫히면 포커스가 이전 위치로 돌아갑니다
- 열려 있는 동안 배경 페이지 스크롤이 잠깁니다

### 키보드 인터랙션

| 키 | 동작 |
|----|------|
| `Tab` | 다이얼로그 내부 포커스를 순환합니다 |
| `Shift + Tab` | 역방향으로 포커스를 순환합니다 |
| `Esc` | 가장 위에 떠 있는 다이얼로그만 닫습니다 |

### ARIA

- `Dialog.Content`에는 `role="dialog"`와 `aria-modal="true"`가 적용됩니다
- `Dialog.Title`를 사용하면 `aria-labelledby`가 자동으로 연결됩니다
- `Dialog.Description`를 사용하면 `aria-describedby`가 자동으로 연결됩니다
