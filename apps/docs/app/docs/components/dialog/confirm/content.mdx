import {
  BasicConfirmDemo,
  CancelConfirmDemo,
  AsyncConfirmDemo,
  ErrorConfirmDemo,
  RetryConfirmDemo,
} from './demos'

export const toc = [
  { id: '개요', label: '개요', depth: 2 },
  { id: '기본-사용법', label: '기본 사용법', depth: 2 },
  { id: '옵션', label: '옵션', depth: 2 },
  { id: '비동기-플로우', label: '비동기 플로우', depth: 2 },
  { id: '에러-처리', label: '에러 처리', depth: 2 },
  { id: 'confirmrendercontext', label: 'ConfirmRenderContext', depth: 2 },
  { id: '커스텀-렌더링', label: '커스텀 렌더링', depth: 2 },
  { id: '접근성', label: '접근성', depth: 2 },
]

## 개요

```ts
import { confirm } from '@woon/core/dialog'
```

`confirm()`은 사용자의 확인/취소 선택을 받는 함수입니다.
`Promise<DialogConfirmResult>`를 반환하며, `onConfirm`에 비동기 함수를 전달하면
**confirm → loading → success/error** 플로우가 자동으로 동작합니다.

`WoonProvider` 없이 어디서든 호출할 수 있습니다.

## 기본 사용법

```tsx
import { confirm } from '@woon/core/dialog'

const result = await confirm({
  title: '삭제하시겠습니까?',
  description: '이 작업은 되돌릴 수 없습니다.',
  tone: 'danger',
})

if (result.status === 'confirmed') {
  await deleteItem()
} else if (result.status === 'cancelled') {
  // 취소 버튼 클릭
}
```

### DialogConfirmResult

| status | 설명 |
|--------|------|
| `'confirmed'` | 확인 버튼 클릭 |
| `'cancelled'` | 취소 버튼 클릭 |
| `'dismissed'` | ESC로 닫힘 |

<DemoBox>
  <BasicConfirmDemo />
  <CancelConfirmDemo />
</DemoBox>

## 옵션

| 옵션 | 타입 | 기본값 | 설명 |
|------|------|--------|------|
| `title` | `ReactNode` | — | 제목 |
| `description` | `ReactNode` | — | 설명 |
| `confirmLabel` | `ReactNode` | `'확인'` | 확인 버튼 텍스트 |
| `cancelLabel` | `ReactNode` | `'취소'` | 취소 버튼 텍스트 |
| `tone` | `'default'` 또는 `'danger'` | `'default'` | 시각적 톤 |
| `onConfirm` | `() => void \| Promise<void>` | — | 확인 시 실행할 핸들러 |
| `loading` | `MessageStep` | — | loading step 표시값 |
| `success` | `MessageStep` | — | success step 표시값 |
| `error` | `MessageStep` 또는 함수 | — | error step 표시값 |
| `retryOnError` | `boolean` | `false` | 에러 시 confirm step으로 되돌아감 |
| `render` | `(ctx: ConfirmRenderContext) => ReactNode` | — | 커스텀 렌더 함수 |

`DialogOptions` (`overlay`, `trapFocus`, `scrollLock` 등)도 모두 전달할 수 있습니다.

<Callout type="note">
  `closeOnOverlayClick`은 기본값이 `false`입니다. 실수로 닫히는 것을 방지합니다.
</Callout>

## 비동기 플로우

`onConfirm`에 `Promise`를 반환하는 함수를 전달하면 단계별 UI가 자동으로 전환됩니다.

```
확인 버튼 클릭
    ↓
loading step  ← onConfirm() 실행 중
    ↓
success step  ← onConfirm() 성공 (success 설정 시)
    ↓
다이얼로그 닫힘, result.status === 'confirmed'
```

```tsx
await confirm({
  title: '배포하시겠습니까?',
  onConfirm: async () => {
    await deployToProduction()
  },
  loading: { title: '배포 중입니다', description: '잠시만 기다려주세요.' },
  success: { title: '배포 완료', description: '정상적으로 배포되었습니다.' },
})
```

`success`를 설정하지 않으면 `onConfirm` 성공 직후 바로 닫힙니다.

<DemoBox>
  <AsyncConfirmDemo />
</DemoBox>

### 기본 라벨

`onConfirm`이 있을 때 각 step의 기본값입니다.

| Step | title | confirmLabel | cancelLabel |
|------|-------|--------------|-------------|
| confirm | (사용자 지정) | 확인 | 취소 |
| loading | 처리 중입니다 | 처리 중 | — |
| success | 완료되었습니다 | 확인 | — |
| error | 처리하지 못했습니다 | 다시 시도 | 닫기 |

### MessageStep 타입

```ts
type MessageStep = {
  title?: ReactNode
  description?: ReactNode
  confirmLabel?: ReactNode
  cancelLabel?: ReactNode
}
```

## 에러 처리

`onConfirm`이 throw하면 error step으로 전환됩니다.

```
onConfirm() throw
    ↓
error step  ← "다시 시도" 또는 "닫기" 선택
```

```tsx
await confirm({
  title: '전송하시겠습니까?',
  onConfirm: async () => {
    const res = await fetch('/api/send')
    if (!res.ok) throw new Error('전송 실패')
  },
  error: { title: '전송 실패', description: '잠시 후 다시 시도해주세요.' },
})
```

에러 내용에 따라 다른 메시지를 표시하려면 함수 형태를 사용합니다.

```tsx
error: (err) => ({
  title: '오류 발생',
  description: err instanceof Error ? err.message : '알 수 없는 오류',
})
```

### retryOnError

`retryOnError: true`를 설정하면 에러 시 error step 대신 confirm step으로 돌아갑니다.
사용자가 직접 다시 시도 버튼을 누를 수 있습니다.

```tsx
await confirm({
  title: '전송하시겠습니까?',
  onConfirm: sendData,
  retryOnError: true,
})
```

<DemoBox>
  <ErrorConfirmDemo />
  <RetryConfirmDemo />
</DemoBox>

## ConfirmRenderContext

`render` 옵션 또는 `dialogPlugin` 등록 컴포넌트가 받는 컨텍스트입니다.
현재 step에 맞는 표시값이 미리 계산되어 있어 그대로 사용할 수 있습니다.

| 속성 | 타입 | 설명 |
|------|------|------|
| `options` | `DialogConfirmOptions` | 원본 옵션 |
| `step` | `'confirm'` `'loading'` `'success'` `'error'` | 현재 단계 |
| `error` | `unknown` | 에러 객체 (error step일 때) |
| `onConfirm` | `() => void` | 확인 핸들러 |
| `onCancel` | `() => void` | 취소 핸들러 |
| `title` | `ReactNode` | 현재 step의 제목 |
| `description` | `ReactNode` | 현재 step의 설명 |
| `confirmLabel` | `ReactNode` | 현재 step의 확인 라벨 |
| `cancelLabel` | `ReactNode` | 현재 step의 취소 라벨 |
| `showCancel` | `boolean` | 취소 버튼 표시 여부 |

## 커스텀 렌더링

### render prop

```tsx
await confirm({
  title: '삭제',
  render: (ctx) => (
    <div>
      <p>{ctx.title}</p>
      <button onClick={ctx.onConfirm} disabled={ctx.step === 'loading'}>
        {ctx.confirmLabel}
      </button>
      {ctx.showCancel && (
        <button onClick={ctx.onCancel}>{ctx.cancelLabel}</button>
      )}
    </div>
  ),
})
```

### dialogPlugin 전역 등록

```tsx
import { dialogPlugin } from '@woon/core/dialog'
import { MyConfirm } from './ui/Confirm'

dialogPlugin({ render: { confirm: MyConfirm } })
```

`MyConfirm`은 `ConfirmRenderContext`를 props로 받는 컴포넌트입니다.

<Callout type="tip">
  **렌더 우선순위** (높음 → 낮음): `render` prop → `dialogPlugin` 등록 컴포넌트 → 내장 기본 UI
</Callout>

## 접근성

- `role="dialog"`, `aria-modal="true"` 자동 적용
- loading step 중 확인 버튼 비활성화 (이중 제출 방지)
- ESC로 닫기 가능 (loading step 중에는 ESC 무시)
- 닫힐 때 이전 포커스 요소로 복귀
