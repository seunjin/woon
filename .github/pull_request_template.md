## 요약

-

## 검증

-

## PR 규칙

- 제목과 본문은 한국어로 작성합니다.
- 제목에 `[codex]`, `[claude]` 같은 에이전트 접두어를 붙이지 않습니다.
- 브랜치명에 `codex/`, `claude/` 같은 에이전트 접두어를 쓰지 않습니다.
- 브랜치명은 작업 성격을 나타내는 접두어를 사용합니다. 예: `docs/`, `feat/`, `fix/`, `refactor/`, `chore/`
- 기본은 draft PR입니다. ready PR은 명시적으로 요청받은 경우에만 생성합니다.
